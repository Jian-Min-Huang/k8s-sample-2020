{{ $activeProfiles := .Values.activeProfiles }}

apiVersion: v1
data:
  TZ: Asia/Taipei
  activeProfiles: {{ $activeProfiles }}
kind: ConfigMap
metadata:
  labels:
    app: sample-pt-gw-{{ $activeProfiles }}
  name: sample-pt-gw-config
