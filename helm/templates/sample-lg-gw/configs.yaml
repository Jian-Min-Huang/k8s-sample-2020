{{ $activeProfiles := .Values.activeProfiles }}

apiVersion: v1
data:
  TZ: Asia/Taipei
  activeProfiles: {{ $activeProfiles }}
kind: ConfigMap
metadata:
  labels:
    app: sample-lg-gw-{{ $activeProfiles }}
  name: sample-lg-gw-config
